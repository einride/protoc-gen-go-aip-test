// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package databasepb

import (
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	proto "google.golang.org/protobuf/proto"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	fieldmaskpb "google.golang.org/protobuf/types/known/fieldmaskpb"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
)

// DatabaseAdminTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type DatabaseAdminTestSuiteConfigProvider interface {
	// DatabaseAdminBackup should return a config, or nil, which means that the tests will be skipped.
	DatabaseAdminBackup(t *testing.T) *DatabaseAdminBackupTestSuiteConfig
	// DatabaseAdminDatabase should return a config, or nil, which means that the tests will be skipped.
	DatabaseAdminDatabase(t *testing.T) *DatabaseAdminDatabaseTestSuiteConfig
	// DatabaseAdminDatabaseRole should return a config, or nil, which means that the tests will be skipped.
	DatabaseAdminDatabaseRole(t *testing.T) *DatabaseAdminDatabaseRoleTestSuiteConfig
}

// testDatabaseAdmin is the main entrypoint for starting the AIP tests.
func testDatabaseAdmin(t *testing.T, s DatabaseAdminTestSuiteConfigProvider) {
	testDatabaseAdminBackup(t, s)
	testDatabaseAdminDatabase(t, s)
	testDatabaseAdminDatabaseRole(t, s)
}

func testDatabaseAdminBackup(t *testing.T, s DatabaseAdminTestSuiteConfigProvider) {
	t.Run("Backup", func(t *testing.T) {
		config := s.DatabaseAdminBackup(t)
		if config == nil {
			t.Skip("Method DatabaseAdminBackup not implemented")
		}
		if config.Service == nil {
			t.Skip("Method DatabaseAdminBackup.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testDatabaseAdminDatabase(t *testing.T, s DatabaseAdminTestSuiteConfigProvider) {
	t.Run("Database", func(t *testing.T) {
		config := s.DatabaseAdminDatabase(t)
		if config == nil {
			t.Skip("Method DatabaseAdminDatabase not implemented")
		}
		if config.Service == nil {
			t.Skip("Method DatabaseAdminDatabase.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testDatabaseAdminDatabaseRole(t *testing.T, s DatabaseAdminTestSuiteConfigProvider) {
	t.Run("DatabaseRole", func(t *testing.T) {
		config := s.DatabaseAdminDatabaseRole(t)
		if config == nil {
			t.Skip("Method DatabaseAdminDatabaseRole not implemented")
		}
		if config.Service == nil {
			t.Skip("Method DatabaseAdminDatabaseRole.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type DatabaseAdminTestSuite struct {
	T *testing.T
	// Server to test.
	Server DatabaseAdminServer
}

func (fx DatabaseAdminTestSuite) TestBackup(ctx context.Context, options DatabaseAdminBackupTestSuiteConfig) {
	fx.T.Run("Backup", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() DatabaseAdminServer { return fx.Server }
		options.test(t)
	})
}

func (fx DatabaseAdminTestSuite) TestDatabase(ctx context.Context, options DatabaseAdminDatabaseTestSuiteConfig) {
	fx.T.Run("Database", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() DatabaseAdminServer { return fx.Server }
		options.test(t)
	})
}

func (fx DatabaseAdminTestSuite) TestDatabaseRole(ctx context.Context, options DatabaseAdminDatabaseRoleTestSuiteConfig) {
	fx.T.Run("DatabaseRole", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() DatabaseAdminServer { return fx.Server }
		options.test(t)
	})
}

type DatabaseAdminBackupTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() DatabaseAdminServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *Backup
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *Backup
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *DatabaseAdminBackupTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *DatabaseAdminBackupTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateBackupRequest{}
		req.SetParent("")
		req.SetBackup(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateBackupRequest{}
		req.SetParent("invalid resource name")
		req.SetBackup(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".database", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.SetDatabase("invalid resource name")
			req := &CreateBackupRequest{}
			req.SetParent(parent)
			req.SetBackup(msg)
			_, err := fx.Service().CreateBackup(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *DatabaseAdminBackupTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetBackupRequest{}
		req.SetName("")
		_, err := fx.Service().GetBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetBackupRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetBackupRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetBackup(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetBackupRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetBackup(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetBackupRequest{}
		req.SetName("projects/-/instances/-/backups/-")
		_, err := fx.Service().GetBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *DatabaseAdminBackupTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateBackupRequest{}
		req.SetBackup(msg)
		_, err := fx.Service().UpdateBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateBackupRequest{}
		req.SetBackup(msg)
		_, err := fx.Service().UpdateBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The updated resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		updateReq := &UpdateBackupRequest{}
		updateReq.SetBackup(created)
		updated, err := fx.Service().UpdateBackup(fx.Context(), updateReq)
		assert.NilError(t, err)
		getReq := &GetBackupRequest{}
		getReq.SetName(updated.GetName())
		persisted, err := fx.Service().GetBackup(fx.Context(), getReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, updated, persisted, protocmp.Transform())
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateBackupRequest{}
			req.SetBackup(msg)
			_, err := fx.Service().UpdateBackup(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateBackupRequest{}
			req.SetBackup(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateBackup(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

	}
}

func (fx *DatabaseAdminBackupTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListBackupsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListBackups(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListBackupsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListBackups(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListBackupsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListBackups(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Backup, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListBackupsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListBackups(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetBackups(),
				cmpopts.SortSlices(func(a, b *Backup) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListBackupsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListBackups(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListBackupsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListBackups(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Backup, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListBackupsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListBackups(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetBackups()))
				msgs = append(msgs, response.GetBackups()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Backup) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteBackupRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteBackup(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListBackupsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListBackups(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetBackups(),
				cmpopts.SortSlices(func(a, b *Backup) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Backup, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Backup, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListBackupsRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListBackups(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetBackups()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Backup) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *DatabaseAdminBackupTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteBackupRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteBackupRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteBackupRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteBackup(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteBackupRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteBackup(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteBackupRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteBackup(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteBackupRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteBackup(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteBackupRequest{}
		req.SetName("projects/-/instances/-/backups/-")
		_, err := fx.Service().DeleteBackup(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *DatabaseAdminBackupTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *DatabaseAdminBackupTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *DatabaseAdminBackupTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *DatabaseAdminBackupTestSuiteConfig) create(t *testing.T, parent string) *Backup {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}

type DatabaseAdminDatabaseTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() DatabaseAdminServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// CreateResource should create a Database and return it.
	// If the field is not set, some tests will be skipped.
	//
	// This method is generated because service does not expose a Create
	// method (or it does not comply with AIP).
	CreateResource func(ctx context.Context, parent string) (*Database, error)
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *Database
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) test(t *testing.T) {
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDatabaseRequest{}
		req.SetName("")
		_, err := fx.Service().GetDatabase(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDatabaseRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetDatabase(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetDatabaseRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetDatabase(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetDatabaseRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetDatabase(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDatabaseRequest{}
		req.SetName("projects/-/instances/-/databases/-")
		_, err := fx.Service().GetDatabase(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateDatabaseRequest{}
		req.SetDatabase(msg)
		_, err := fx.Service().UpdateDatabase(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateDatabaseRequest{}
		req.SetDatabase(msg)
		_, err := fx.Service().UpdateDatabase(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateDatabaseRequest{}
			req.SetDatabase(msg)
			_, err := fx.Service().UpdateDatabase(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateDatabaseRequest{}
			req.SetDatabase(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateDatabase(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Database)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateDatabaseRequest{}
				req.SetDatabase(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateDatabase(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListDatabasesRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListDatabases(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDatabasesRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListDatabases(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDatabasesRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListDatabases(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Database, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDatabasesRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListDatabases(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetDatabases(),
				cmpopts.SortSlices(func(a, b *Database) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDatabasesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListDatabases(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDatabasesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListDatabases(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Database, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDatabasesRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListDatabases(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetDatabases()))
				msgs = append(msgs, response.GetDatabases()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Database) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Database, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Database, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDatabasesRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListDatabases(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetDatabases()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Database) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *DatabaseAdminDatabaseTestSuiteConfig) create(t *testing.T, parent string) *Database {
	t.Helper()
	if fx.CreateResource == nil {
		t.Skip("Test skipped because CreateResource not specified on DatabaseAdminDatabaseTestSuiteConfig")
	}
	created, err := fx.CreateResource(fx.Context(), parent)
	assert.NilError(t, err)
	return created
}

type DatabaseAdminDatabaseRoleTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() DatabaseAdminServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// CreateResource should create a DatabaseRole and return it.
	// If the field is not set, some tests will be skipped.
	//
	// This method is generated because service does not expose a Create
	// method (or it does not comply with AIP).
	CreateResource func(ctx context.Context, parent string) (*DatabaseRole, error)
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *DatabaseAdminDatabaseRoleTestSuiteConfig) test(t *testing.T) {
	t.Run("List", fx.testList)
}

func (fx *DatabaseAdminDatabaseRoleTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListDatabaseRolesRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDatabaseRolesRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDatabaseRolesRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*DatabaseRole, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDatabaseRolesRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetDatabaseRoles(),
				cmpopts.SortSlices(func(a, b *DatabaseRole) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDatabaseRolesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDatabaseRolesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*DatabaseRole, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDatabaseRolesRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetDatabaseRoles()))
				msgs = append(msgs, response.GetDatabaseRoles()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *DatabaseRole) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*DatabaseRole, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*DatabaseRole, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDatabaseRolesRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListDatabaseRoles(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetDatabaseRoles()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *DatabaseRole) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *DatabaseAdminDatabaseRoleTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *DatabaseAdminDatabaseRoleTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *DatabaseAdminDatabaseRoleTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *DatabaseAdminDatabaseRoleTestSuiteConfig) create(t *testing.T, parent string) *DatabaseRole {
	t.Helper()
	if fx.CreateResource == nil {
		t.Skip("Test skipped because CreateResource not specified on DatabaseAdminDatabaseRoleTestSuiteConfig")
	}
	created, err := fx.CreateResource(fx.Context(), parent)
	assert.NilError(t, err)
	return created
}
