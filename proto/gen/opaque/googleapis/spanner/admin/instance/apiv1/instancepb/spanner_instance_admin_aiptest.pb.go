// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package instancepb

import (
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	proto "google.golang.org/protobuf/proto"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	fieldmaskpb "google.golang.org/protobuf/types/known/fieldmaskpb"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
)

// InstanceAdminTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type InstanceAdminTestSuiteConfigProvider interface {
	// InstanceAdminInstance should return a config, or nil, which means that the tests will be skipped.
	InstanceAdminInstance(t *testing.T) *InstanceAdminInstanceTestSuiteConfig
	// InstanceAdminInstanceConfig should return a config, or nil, which means that the tests will be skipped.
	InstanceAdminInstanceConfig(t *testing.T) *InstanceAdminInstanceConfigTestSuiteConfig
	// InstanceAdminInstancePartition should return a config, or nil, which means that the tests will be skipped.
	InstanceAdminInstancePartition(t *testing.T) *InstanceAdminInstancePartitionTestSuiteConfig
}

// testInstanceAdmin is the main entrypoint for starting the AIP tests.
func testInstanceAdmin(t *testing.T, s InstanceAdminTestSuiteConfigProvider) {
	testInstanceAdminInstance(t, s)
	testInstanceAdminInstanceConfig(t, s)
	testInstanceAdminInstancePartition(t, s)
}

func testInstanceAdminInstance(t *testing.T, s InstanceAdminTestSuiteConfigProvider) {
	t.Run("Instance", func(t *testing.T) {
		config := s.InstanceAdminInstance(t)
		if config == nil {
			t.Skip("Method InstanceAdminInstance not implemented")
		}
		if config.Service == nil {
			t.Skip("Method InstanceAdminInstance.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testInstanceAdminInstanceConfig(t *testing.T, s InstanceAdminTestSuiteConfigProvider) {
	t.Run("InstanceConfig", func(t *testing.T) {
		config := s.InstanceAdminInstanceConfig(t)
		if config == nil {
			t.Skip("Method InstanceAdminInstanceConfig not implemented")
		}
		if config.Service == nil {
			t.Skip("Method InstanceAdminInstanceConfig.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testInstanceAdminInstancePartition(t *testing.T, s InstanceAdminTestSuiteConfigProvider) {
	t.Run("InstancePartition", func(t *testing.T) {
		config := s.InstanceAdminInstancePartition(t)
		if config == nil {
			t.Skip("Method InstanceAdminInstancePartition not implemented")
		}
		if config.Service == nil {
			t.Skip("Method InstanceAdminInstancePartition.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type InstanceAdminTestSuite struct {
	T *testing.T
	// Server to test.
	Server InstanceAdminServer
}

func (fx InstanceAdminTestSuite) TestInstance(ctx context.Context, options InstanceAdminInstanceTestSuiteConfig) {
	fx.T.Run("Instance", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() InstanceAdminServer { return fx.Server }
		options.test(t)
	})
}

func (fx InstanceAdminTestSuite) TestInstanceConfig(ctx context.Context, options InstanceAdminInstanceConfigTestSuiteConfig) {
	fx.T.Run("InstanceConfig", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() InstanceAdminServer { return fx.Server }
		options.test(t)
	})
}

func (fx InstanceAdminTestSuite) TestInstancePartition(ctx context.Context, options InstanceAdminInstancePartitionTestSuiteConfig) {
	fx.T.Run("InstancePartition", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() InstanceAdminServer { return fx.Server }
		options.test(t)
	})
}

type InstanceAdminInstanceTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() InstanceAdminServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *Instance
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *Instance
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *InstanceAdminInstanceTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *InstanceAdminInstanceTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateInstanceRequest{}
		req.SetParent("")
		req.SetInstance(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateInstanceRequest{}
		req.SetParent("invalid resource name")
		req.SetInstance(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("name")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("config")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".autoscaling_config.autoscaling_limits", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAutoscalingConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("autoscaling_limits")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".autoscaling_config.autoscaling_targets", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAutoscalingConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("autoscaling_targets")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".autoscaling_config.autoscaling_targets.high_priority_cpu_utilization_percent", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAutoscalingConfig().GetAutoscalingTargets()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("high_priority_cpu_utilization_percent")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".autoscaling_config.autoscaling_targets.storage_utilization_percent", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAutoscalingConfig().GetAutoscalingTargets()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("storage_utilization_percent")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.SetConfig("invalid resource name")
			req := &CreateInstanceRequest{}
			req.SetParent(parent)
			req.SetInstance(msg)
			_, err := fx.Service().CreateInstance(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *InstanceAdminInstanceTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstanceRequest{}
		req.SetName("")
		_, err := fx.Service().GetInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstanceRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstanceRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetInstance(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstanceRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetInstance(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstanceRequest{}
		req.SetName("projects/-/instances/-")
		_, err := fx.Service().GetInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *InstanceAdminInstanceTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateInstanceRequest{}
		req.SetInstance(msg)
		_, err := fx.Service().UpdateInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateInstanceRequest{}
		req.SetInstance(msg)
		_, err := fx.Service().UpdateInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateInstanceRequest{}
			req.SetInstance(msg)
			_, err := fx.Service().UpdateInstance(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".config", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("config")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".autoscaling_config.autoscaling_limits", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg.GetAutoscalingConfig()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("autoscaling_limits")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".autoscaling_config.autoscaling_targets", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg.GetAutoscalingConfig()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("autoscaling_targets")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".autoscaling_config.autoscaling_targets.high_priority_cpu_utilization_percent", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg.GetAutoscalingConfig().GetAutoscalingTargets()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("high_priority_cpu_utilization_percent")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".autoscaling_config.autoscaling_targets.storage_utilization_percent", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Instance)
				container := msg.GetAutoscalingConfig().GetAutoscalingTargets()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("storage_utilization_percent")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstanceRequest{}
				req.SetInstance(msg)
				_, err := fx.Service().UpdateInstance(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *InstanceAdminInstanceTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListInstancesRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListInstances(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListInstancesRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListInstances(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListInstancesRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListInstances(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Instance, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancesRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListInstances(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetInstances(),
				cmpopts.SortSlices(func(a, b *Instance) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListInstances(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListInstances(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Instance, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListInstancesRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListInstances(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetInstances()))
				msgs = append(msgs, response.GetInstances()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Instance) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// When listing resource with page size zero the service should use a default value.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancesRequest{}
			req.SetParent(parent)
			req.SetPageSize(0)
			response, err := fx.Service().ListInstances(fx.Context(), req)
			assert.NilError(t, err)
			// Server should use a default page size and return at least some results
			assert.Check(t, len(response.GetInstances()) > 0, "expected server to return at least 1 resource with page_size=0")
		})

	}
	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Instance, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 3
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteInstanceRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteInstance(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListInstancesRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListInstances(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetInstances(),
				cmpopts.SortSlices(func(a, b *Instance) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *InstanceAdminInstanceTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstanceRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstanceRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstanceRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteInstance(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstanceRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteInstance(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteInstanceRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteInstance(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteInstanceRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteInstance(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstanceRequest{}
		req.SetName("projects/-/instances/-")
		_, err := fx.Service().DeleteInstance(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *InstanceAdminInstanceTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *InstanceAdminInstanceTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *InstanceAdminInstanceTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *InstanceAdminInstanceTestSuiteConfig) create(t *testing.T, parent string) *Instance {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}

type InstanceAdminInstanceConfigTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() InstanceAdminServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *InstanceConfig
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *InstanceConfig
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateInstanceConfigRequest{}
		req.SetParent("")
		req.SetInstanceConfig(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateInstanceConfigRequest{}
		req.SetParent("invalid resource name")
		req.SetInstanceConfig(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".base_config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.SetBaseConfig("invalid resource name")
			req := &CreateInstanceConfigRequest{}
			req.SetParent(parent)
			req.SetInstanceConfig(msg)
			_, err := fx.Service().CreateInstanceConfig(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstanceConfigRequest{}
		req.SetName("")
		_, err := fx.Service().GetInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstanceConfigRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstanceConfigRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetInstanceConfig(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstanceConfigRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstanceConfigRequest{}
		req.SetName("projects/-/instanceConfigs/-")
		_, err := fx.Service().GetInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateInstanceConfigRequest{}
		req.SetInstanceConfig(msg)
		_, err := fx.Service().UpdateInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateInstanceConfigRequest{}
		req.SetInstanceConfig(msg)
		_, err := fx.Service().UpdateInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		req := &UpdateInstanceConfigRequest{}
		req.SetInstanceConfig(msg)
		_, err := fx.Service().UpdateInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		req := &UpdateInstanceConfigRequest{}
		req.SetInstanceConfig(msg)
		updated, err := fx.Service().UpdateInstanceConfig(fx.Context(), req)
		assert.NilError(t, err)
		_ = updated
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateInstanceConfigRequest{}
			req.SetInstanceConfig(msg)
			_, err := fx.Service().UpdateInstanceConfig(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateInstanceConfigRequest{}
			req.SetInstanceConfig(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateInstanceConfig(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

	}
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListInstanceConfigsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListInstanceConfigsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListInstanceConfigsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*InstanceConfig, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstanceConfigsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetInstanceConfigs(),
				cmpopts.SortSlices(func(a, b *InstanceConfig) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstanceConfigsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstanceConfigsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*InstanceConfig, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListInstanceConfigsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetInstanceConfigs()))
				msgs = append(msgs, response.GetInstanceConfigs()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *InstanceConfig) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// When listing resource with page size zero the service should use a default value.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstanceConfigsRequest{}
			req.SetParent(parent)
			req.SetPageSize(0)
			response, err := fx.Service().ListInstanceConfigs(fx.Context(), req)
			assert.NilError(t, err)
			// Server should use a default page size and return at least some results
			assert.Check(t, len(response.GetInstanceConfigs()) > 0, "expected server to return at least 1 resource with page_size=0")
		})

	}
	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*InstanceConfig, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 3
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteInstanceConfigRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteInstanceConfig(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListInstanceConfigsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListInstanceConfigs(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetInstanceConfigs(),
				cmpopts.SortSlices(func(a, b *InstanceConfig) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstanceConfigRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstanceConfigRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstanceConfigRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteInstanceConfig(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstanceConfigRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteInstanceConfigRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteInstanceConfig(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteInstanceConfigRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteInstanceConfig(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstanceConfigRequest{}
		req.SetName("projects/-/instanceConfigs/-")
		_, err := fx.Service().DeleteInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstanceConfigRequest{}
		req.SetName(created.GetName())
		req.SetEtag(`"99999"`)
		_, err := fx.Service().DeleteInstanceConfig(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *InstanceAdminInstanceConfigTestSuiteConfig) create(t *testing.T, parent string) *InstanceConfig {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}

type InstanceAdminInstancePartitionTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() InstanceAdminServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *InstancePartition
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *InstancePartition
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateInstancePartitionRequest{}
		req.SetParent("")
		req.SetInstancePartition(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateInstancePartitionRequest{}
		req.SetParent("invalid resource name")
		req.SetInstancePartition(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("name")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstancePartitionRequest{}
			req.SetParent(parent)
			req.SetInstancePartition(msg)
			_, err := fx.Service().CreateInstancePartition(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("config")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstancePartitionRequest{}
			req.SetParent(parent)
			req.SetInstancePartition(msg)
			_, err := fx.Service().CreateInstancePartition(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateInstancePartitionRequest{}
			req.SetParent(parent)
			req.SetInstancePartition(msg)
			_, err := fx.Service().CreateInstancePartition(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".config", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			container.SetConfig("invalid resource name")
			req := &CreateInstancePartitionRequest{}
			req.SetParent(parent)
			req.SetInstancePartition(msg)
			_, err := fx.Service().CreateInstancePartition(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstancePartitionRequest{}
		req.SetName("")
		_, err := fx.Service().GetInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstancePartitionRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstancePartitionRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetInstancePartition(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstancePartitionRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstancePartitionRequest{}
		req.SetName("projects/-/instances/-/instancePartitions/-")
		_, err := fx.Service().GetInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateInstancePartitionRequest{}
		req.SetInstancePartition(msg)
		_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateInstancePartitionRequest{}
		req.SetInstancePartition(msg)
		_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		req := &UpdateInstancePartitionRequest{}
		req.SetInstancePartition(msg)
		_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		req := &UpdateInstancePartitionRequest{}
		req.SetInstancePartition(msg)
		updated, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
		assert.NilError(t, err)
		_ = updated
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateInstancePartitionRequest{}
			req.SetInstancePartition(msg)
			_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*InstancePartition)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstancePartitionRequest{}
				req.SetInstancePartition(msg)
				_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".config", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*InstancePartition)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("config")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstancePartitionRequest{}
				req.SetInstancePartition(msg)
				_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*InstancePartition)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateInstancePartitionRequest{}
				req.SetInstancePartition(msg)
				_, err := fx.Service().UpdateInstancePartition(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListInstancePartitionsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListInstancePartitions(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListInstancePartitionsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListInstancePartitions(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListInstancePartitionsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListInstancePartitions(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*InstancePartition, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancePartitionsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListInstancePartitions(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetInstancePartitions(),
				cmpopts.SortSlices(func(a, b *InstancePartition) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancePartitionsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListInstancePartitions(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancePartitionsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListInstancePartitions(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*InstancePartition, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListInstancePartitionsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListInstancePartitions(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetInstancePartitions()))
				msgs = append(msgs, response.GetInstancePartitions()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *InstancePartition) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// When listing resource with page size zero the service should use a default value.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListInstancePartitionsRequest{}
			req.SetParent(parent)
			req.SetPageSize(0)
			response, err := fx.Service().ListInstancePartitions(fx.Context(), req)
			assert.NilError(t, err)
			// Server should use a default page size and return at least some results
			assert.Check(t, len(response.GetInstancePartitions()) > 0, "expected server to return at least 1 resource with page_size=0")
		})

	}
	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*InstancePartition, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 3
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteInstancePartitionRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteInstancePartition(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListInstancePartitionsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListInstancePartitions(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetInstancePartitions(),
				cmpopts.SortSlices(func(a, b *InstancePartition) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstancePartitionRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstancePartitionRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstancePartitionRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteInstancePartition(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstancePartitionRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteInstancePartitionRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteInstancePartition(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteInstancePartitionRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteInstancePartition(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteInstancePartitionRequest{}
		req.SetName("projects/-/instances/-/instancePartitions/-")
		_, err := fx.Service().DeleteInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteInstancePartitionRequest{}
		req.SetName(created.GetName())
		req.SetEtag(`"99999"`)
		_, err := fx.Service().DeleteInstancePartition(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *InstanceAdminInstancePartitionTestSuiteConfig) create(t *testing.T, parent string) *InstancePartition {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}
