// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package aiplatformpb

import (
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
)

// DeploymentResourcePoolServiceTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type DeploymentResourcePoolServiceTestSuiteConfigProvider interface {
	// DeploymentResourcePoolServiceDeploymentResourcePool should return a config, or nil, which means that the tests will be skipped.
	DeploymentResourcePoolServiceDeploymentResourcePool(t *testing.T) *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig
}

// testDeploymentResourcePoolService is the main entrypoint for starting the AIP tests.
func testDeploymentResourcePoolService(t *testing.T, s DeploymentResourcePoolServiceTestSuiteConfigProvider) {
	testDeploymentResourcePoolServiceDeploymentResourcePool(t, s)
}

func testDeploymentResourcePoolServiceDeploymentResourcePool(t *testing.T, s DeploymentResourcePoolServiceTestSuiteConfigProvider) {
	t.Run("DeploymentResourcePool", func(t *testing.T) {
		config := s.DeploymentResourcePoolServiceDeploymentResourcePool(t)
		if config == nil {
			t.Skip("Method DeploymentResourcePoolServiceDeploymentResourcePool not implemented")
		}
		if config.Service == nil {
			t.Skip("Method DeploymentResourcePoolServiceDeploymentResourcePool.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type DeploymentResourcePoolServiceTestSuite struct {
	T *testing.T
	// Server to test.
	Server DeploymentResourcePoolServiceServer
}

func (fx DeploymentResourcePoolServiceTestSuite) TestDeploymentResourcePool(ctx context.Context, options DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) {
	fx.T.Run("DeploymentResourcePool", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() DeploymentResourcePoolServiceServer { return fx.Server }
		options.test(t)
	})
}

type DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() DeploymentResourcePoolServiceServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *DeploymentResourcePool
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateDeploymentResourcePoolRequest{}
		req.SetParent("")
		req.SetDeploymentResourcePool(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateDeploymentResourcePoolRequest{}
		req.SetParent("invalid resource name")
		req.SetDeploymentResourcePool(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".dedicated_resources", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("dedicated_resources")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentResourcePoolRequest{}
			req.SetParent(parent)
			req.SetDeploymentResourcePool(msg)
			_, err := fx.Service().CreateDeploymentResourcePool(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".dedicated_resources.machine_spec", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetDedicatedResources()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("machine_spec")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentResourcePoolRequest{}
			req.SetParent(parent)
			req.SetDeploymentResourcePool(msg)
			_, err := fx.Service().CreateDeploymentResourcePool(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".dedicated_resources.min_replica_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetDedicatedResources()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("min_replica_count")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentResourcePoolRequest{}
			req.SetParent(parent)
			req.SetDeploymentResourcePool(msg)
			_, err := fx.Service().CreateDeploymentResourcePool(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDeploymentResourcePoolRequest{}
		req.SetName("")
		_, err := fx.Service().GetDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDeploymentResourcePoolRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetDeploymentResourcePoolRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetDeploymentResourcePool(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetDeploymentResourcePoolRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDeploymentResourcePoolRequest{}
		req.SetName("projects/-/locations/-/deploymentResourcePools/-")
		_, err := fx.Service().GetDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListDeploymentResourcePoolsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDeploymentResourcePoolsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDeploymentResourcePoolsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*DeploymentResourcePool, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentResourcePoolsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetDeploymentResourcePools(),
				cmpopts.SortSlices(func(a, b *DeploymentResourcePool) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentResourcePoolsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentResourcePoolsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*DeploymentResourcePool, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDeploymentResourcePoolsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetDeploymentResourcePools()))
				msgs = append(msgs, response.GetDeploymentResourcePools()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *DeploymentResourcePool) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// When listing resource with page size zero the service should use a default value.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentResourcePoolsRequest{}
			req.SetParent(parent)
			req.SetPageSize(0)
			response, err := fx.Service().ListDeploymentResourcePools(fx.Context(), req)
			assert.NilError(t, err)
			// Server should use a default page size and return at least some results
			assert.Check(t, len(response.GetDeploymentResourcePools()) > 0, "expected server to return at least 1 resource with page_size=0")
		})

	}
	{
		const resourcesCount = 5
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*DeploymentResourcePool, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 3
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteDeploymentResourcePoolRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListDeploymentResourcePoolsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListDeploymentResourcePools(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetDeploymentResourcePools(),
				cmpopts.SortSlices(func(a, b *DeploymentResourcePool) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteDeploymentResourcePoolRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteDeploymentResourcePoolRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteDeploymentResourcePoolRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteDeploymentResourcePoolRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteDeploymentResourcePoolRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteDeploymentResourcePoolRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteDeploymentResourcePool(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteDeploymentResourcePoolRequest{}
		req.SetName("projects/-/locations/-/deploymentResourcePools/-")
		_, err := fx.Service().DeleteDeploymentResourcePool(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *DeploymentResourcePoolServiceDeploymentResourcePoolTestSuiteConfig) create(t *testing.T, parent string) *DeploymentResourcePool {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}
