// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package aiplatformpb

import (
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	proto "google.golang.org/protobuf/proto"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	fieldmaskpb "google.golang.org/protobuf/types/known/fieldmaskpb"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
	time "time"
)

// ScheduleServiceTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type ScheduleServiceTestSuiteConfigProvider interface {
	// ScheduleServiceSchedule should return a config, or nil, which means that the tests will be skipped.
	ScheduleServiceSchedule(t *testing.T) *ScheduleServiceScheduleTestSuiteConfig
}

// testScheduleService is the main entrypoint for starting the AIP tests.
func testScheduleService(t *testing.T, s ScheduleServiceTestSuiteConfigProvider) {
	testScheduleServiceSchedule(t, s)
}

func testScheduleServiceSchedule(t *testing.T, s ScheduleServiceTestSuiteConfigProvider) {
	t.Run("Schedule", func(t *testing.T) {
		config := s.ScheduleServiceSchedule(t)
		if config == nil {
			t.Skip("Method ScheduleServiceSchedule not implemented")
		}
		if config.Service == nil {
			t.Skip("Method ScheduleServiceSchedule.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type ScheduleServiceTestSuite struct {
	T *testing.T
	// Server to test.
	Server ScheduleServiceServer
}

func (fx ScheduleServiceTestSuite) TestSchedule(ctx context.Context, options ScheduleServiceScheduleTestSuiteConfig) {
	fx.T.Run("Schedule", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() ScheduleServiceServer { return fx.Server }
		options.test(t)
	})
}

type ScheduleServiceScheduleTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() ScheduleServiceServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *Schedule
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *Schedule
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateScheduleRequest{}
		req.SetParent("")
		req.SetSchedule(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateScheduleRequest{}
		req.SetParent("invalid resource name")
		req.SetSchedule(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		req := &CreateScheduleRequest{}
		req.SetParent(parent)
		req.SetSchedule(fx.Create(parent))
		msg, err := fx.Service().CreateSchedule(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, msg.GetCreateTime() != nil)
		assert.Check(t, !msg.GetCreateTime().AsTime().IsZero())
		assert.Check(t, msg.GetCreateTime().AsTime().After(beforeCreate), "msg.GetCreateTime()(%v) is not after beforeCreate (%v)", msg.GetCreateTime().AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		getReq := &CreateScheduleRequest{}
		getReq.SetParent(parent)
		getReq.SetSchedule(fx.Create(parent))
		msg, err := fx.Service().CreateSchedule(fx.Context(), getReq)
		assert.NilError(t, err)
		createReq := &GetScheduleRequest{}
		createReq.SetName(msg.GetName())
		persisted, err := fx.Service().GetSchedule(fx.Context(), createReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".create_pipeline_job_request.parent", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetCreatePipelineJobRequest()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("parent")
			container.ProtoReflect().Clear(fd)
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".create_pipeline_job_request.pipeline_job", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetCreatePipelineJobRequest()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("pipeline_job")
			container.ProtoReflect().Clear(fd)
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".create_pipeline_job_request.pipeline_job.runtime_config.gcs_output_directory", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetCreatePipelineJobRequest().GetPipelineJob().GetRuntimeConfig()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("gcs_output_directory")
			container.ProtoReflect().Clear(fd)
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".create_pipeline_job_request.pipeline_job.encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetCreatePipelineJobRequest().GetPipelineJob().GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".max_concurrent_run_count", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("max_concurrent_run_count")
			container.ProtoReflect().Clear(fd)
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

	// The method should fail with InvalidArgument if the resource has any
	// resource references and they are invalid.
	t.Run("resource references", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".create_pipeline_job_request.parent", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetCreatePipelineJobRequest()
			if container == nil {
				t.Skip("not reachable")
			}
			container.SetParent("invalid resource name")
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".create_pipeline_job_request.pipeline_job.network", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetCreatePipelineJobRequest().GetPipelineJob()
			if container == nil {
				t.Skip("not reachable")
			}
			container.SetNetwork("invalid resource name")
			req := &CreateScheduleRequest{}
			req.SetParent(parent)
			req.SetSchedule(msg)
			_, err := fx.Service().CreateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *ScheduleServiceScheduleTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetScheduleRequest{}
		req.SetName("")
		_, err := fx.Service().GetSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetScheduleRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetScheduleRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetSchedule(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetScheduleRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetSchedule(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetScheduleRequest{}
		req.SetName("projects/-/locations/-/schedules/-")
		_, err := fx.Service().GetSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *ScheduleServiceScheduleTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateScheduleRequest{}
		req.SetSchedule(msg)
		_, err := fx.Service().UpdateSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateScheduleRequest{}
		req.SetSchedule(msg)
		_, err := fx.Service().UpdateSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field update_time should be updated when the resource is updated.
	t.Run("update time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &UpdateScheduleRequest{}
		req.SetSchedule(created)
		updated, err := fx.Service().UpdateSchedule(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetUpdateTime().AsTime().After(created.GetUpdateTime().AsTime()))
	})

	// The updated resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		updateReq := &UpdateScheduleRequest{}
		updateReq.SetSchedule(created)
		updated, err := fx.Service().UpdateSchedule(fx.Context(), updateReq)
		assert.NilError(t, err)
		getReq := &GetScheduleRequest{}
		getReq.SetName(updated.GetName())
		persisted, err := fx.Service().GetSchedule(fx.Context(), getReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, updated, persisted, protocmp.Transform())
	})

	// The field create_time should be preserved when a '*'-update mask is used.
	t.Run("preserve create_time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		originalCreateTime := created.GetCreateTime()
		req := &UpdateScheduleRequest{}
		req.SetSchedule(created)
		req.SetUpdateMask(&fieldmaskpb.FieldMask{
			Paths: []string{
				"*",
			},
		})
		updated, err := fx.Service().UpdateSchedule(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, originalCreateTime, updated.GetCreateTime(), protocmp.Transform())
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateScheduleRequest{}
			req.SetSchedule(msg)
			_, err := fx.Service().UpdateSchedule(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateScheduleRequest{}
			req.SetSchedule(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateSchedule(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".create_pipeline_job_request.parent", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Schedule)
				container := msg.GetCreatePipelineJobRequest()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("parent")
				container.ProtoReflect().Clear(fd)
				req := &UpdateScheduleRequest{}
				req.SetSchedule(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateSchedule(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".create_pipeline_job_request.pipeline_job", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Schedule)
				container := msg.GetCreatePipelineJobRequest()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("pipeline_job")
				container.ProtoReflect().Clear(fd)
				req := &UpdateScheduleRequest{}
				req.SetSchedule(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateSchedule(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".create_pipeline_job_request.pipeline_job.runtime_config.gcs_output_directory", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Schedule)
				container := msg.GetCreatePipelineJobRequest().GetPipelineJob().GetRuntimeConfig()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("gcs_output_directory")
				container.ProtoReflect().Clear(fd)
				req := &UpdateScheduleRequest{}
				req.SetSchedule(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateSchedule(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".create_pipeline_job_request.pipeline_job.encryption_spec.kms_key_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Schedule)
				container := msg.GetCreatePipelineJobRequest().GetPipelineJob().GetEncryptionSpec()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateScheduleRequest{}
				req.SetSchedule(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateSchedule(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Schedule)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateScheduleRequest{}
				req.SetSchedule(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateSchedule(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".max_concurrent_run_count", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Schedule)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("max_concurrent_run_count")
				container.ProtoReflect().Clear(fd)
				req := &UpdateScheduleRequest{}
				req.SetSchedule(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateSchedule(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListSchedulesRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListSchedules(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListSchedulesRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListSchedules(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListSchedulesRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListSchedules(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Schedule, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListSchedulesRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListSchedules(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetSchedules(),
				cmpopts.SortSlices(func(a, b *Schedule) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListSchedulesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListSchedules(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListSchedulesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListSchedules(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Schedule, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListSchedulesRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListSchedules(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetSchedules()))
				msgs = append(msgs, response.GetSchedules()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Schedule) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteScheduleRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteSchedule(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListSchedulesRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListSchedules(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetSchedules(),
				cmpopts.SortSlices(func(a, b *Schedule) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Schedule, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Schedule, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListSchedulesRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListSchedules(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetSchedules()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Schedule) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteScheduleRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteScheduleRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteScheduleRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteSchedule(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteScheduleRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteSchedule(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteScheduleRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteSchedule(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteScheduleRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteSchedule(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteScheduleRequest{}
		req.SetName("projects/-/locations/-/schedules/-")
		_, err := fx.Service().DeleteSchedule(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *ScheduleServiceScheduleTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *ScheduleServiceScheduleTestSuiteConfig) create(t *testing.T, parent string) *Schedule {
	t.Helper()
	req := &CreateScheduleRequest{}
	req.SetParent(parent)
	req.SetSchedule(fx.Create(parent))
	created, err := fx.Service().CreateSchedule(fx.Context(), req)
	assert.NilError(t, err)
	return created
}
