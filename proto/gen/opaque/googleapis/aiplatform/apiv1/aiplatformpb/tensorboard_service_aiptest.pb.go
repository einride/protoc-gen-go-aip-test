// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package aiplatformpb

import (
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	proto "google.golang.org/protobuf/proto"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	fieldmaskpb "google.golang.org/protobuf/types/known/fieldmaskpb"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
	time "time"
)

// TensorboardServiceTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type TensorboardServiceTestSuiteConfigProvider interface {
	// TensorboardServiceTensorboard should return a config, or nil, which means that the tests will be skipped.
	TensorboardServiceTensorboard(t *testing.T) *TensorboardServiceTensorboardTestSuiteConfig
	// TensorboardServiceTensorboardExperiment should return a config, or nil, which means that the tests will be skipped.
	TensorboardServiceTensorboardExperiment(t *testing.T) *TensorboardServiceTensorboardExperimentTestSuiteConfig
	// TensorboardServiceTensorboardRun should return a config, or nil, which means that the tests will be skipped.
	TensorboardServiceTensorboardRun(t *testing.T) *TensorboardServiceTensorboardRunTestSuiteConfig
	// TensorboardServiceTensorboardTimeSeries should return a config, or nil, which means that the tests will be skipped.
	TensorboardServiceTensorboardTimeSeries(t *testing.T) *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig
}

// testTensorboardService is the main entrypoint for starting the AIP tests.
func testTensorboardService(t *testing.T, s TensorboardServiceTestSuiteConfigProvider) {
	testTensorboardServiceTensorboard(t, s)
	testTensorboardServiceTensorboardExperiment(t, s)
	testTensorboardServiceTensorboardRun(t, s)
	testTensorboardServiceTensorboardTimeSeries(t, s)
}

func testTensorboardServiceTensorboard(t *testing.T, s TensorboardServiceTestSuiteConfigProvider) {
	t.Run("Tensorboard", func(t *testing.T) {
		config := s.TensorboardServiceTensorboard(t)
		if config == nil {
			t.Skip("Method TensorboardServiceTensorboard not implemented")
		}
		if config.Service == nil {
			t.Skip("Method TensorboardServiceTensorboard.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testTensorboardServiceTensorboardExperiment(t *testing.T, s TensorboardServiceTestSuiteConfigProvider) {
	t.Run("TensorboardExperiment", func(t *testing.T) {
		config := s.TensorboardServiceTensorboardExperiment(t)
		if config == nil {
			t.Skip("Method TensorboardServiceTensorboardExperiment not implemented")
		}
		if config.Service == nil {
			t.Skip("Method TensorboardServiceTensorboardExperiment.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testTensorboardServiceTensorboardRun(t *testing.T, s TensorboardServiceTestSuiteConfigProvider) {
	t.Run("TensorboardRun", func(t *testing.T) {
		config := s.TensorboardServiceTensorboardRun(t)
		if config == nil {
			t.Skip("Method TensorboardServiceTensorboardRun not implemented")
		}
		if config.Service == nil {
			t.Skip("Method TensorboardServiceTensorboardRun.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testTensorboardServiceTensorboardTimeSeries(t *testing.T, s TensorboardServiceTestSuiteConfigProvider) {
	t.Run("TensorboardTimeSeries", func(t *testing.T) {
		config := s.TensorboardServiceTensorboardTimeSeries(t)
		if config == nil {
			t.Skip("Method TensorboardServiceTensorboardTimeSeries not implemented")
		}
		if config.Service == nil {
			t.Skip("Method TensorboardServiceTensorboardTimeSeries.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type TensorboardServiceTestSuite struct {
	T *testing.T
	// Server to test.
	Server TensorboardServiceServer
}

func (fx TensorboardServiceTestSuite) TestTensorboard(ctx context.Context, options TensorboardServiceTensorboardTestSuiteConfig) {
	fx.T.Run("Tensorboard", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() TensorboardServiceServer { return fx.Server }
		options.test(t)
	})
}

func (fx TensorboardServiceTestSuite) TestTensorboardExperiment(ctx context.Context, options TensorboardServiceTensorboardExperimentTestSuiteConfig) {
	fx.T.Run("TensorboardExperiment", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() TensorboardServiceServer { return fx.Server }
		options.test(t)
	})
}

func (fx TensorboardServiceTestSuite) TestTensorboardRun(ctx context.Context, options TensorboardServiceTensorboardRunTestSuiteConfig) {
	fx.T.Run("TensorboardRun", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() TensorboardServiceServer { return fx.Server }
		options.test(t)
	})
}

func (fx TensorboardServiceTestSuite) TestTensorboardTimeSeries(ctx context.Context, options TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) {
	fx.T.Run("TensorboardTimeSeries", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() TensorboardServiceServer { return fx.Server }
		options.test(t)
	})
}

type TensorboardServiceTensorboardTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() TensorboardServiceServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *Tensorboard
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *Tensorboard
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardRequest{}
		req.SetParent("")
		req.SetTensorboard(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardRequest{}
		req.SetParent("invalid resource name")
		req.SetTensorboard(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateTensorboardRequest{}
			req.SetParent(parent)
			req.SetTensorboard(msg)
			_, err := fx.Service().CreateTensorboard(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetEncryptionSpec()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateTensorboardRequest{}
			req.SetParent(parent)
			req.SetTensorboard(msg)
			_, err := fx.Service().CreateTensorboard(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardRequest{}
		req.SetName("")
		_, err := fx.Service().GetTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetTensorboard(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetTensorboard(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-")
		_, err := fx.Service().GetTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateTensorboardRequest{}
		req.SetTensorboard(msg)
		_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateTensorboardRequest{}
		req.SetTensorboard(msg)
		_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		req := &UpdateTensorboardRequest{}
		req.SetTensorboard(msg)
		_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		req := &UpdateTensorboardRequest{}
		req.SetTensorboard(msg)
		updated, err := fx.Service().UpdateTensorboard(fx.Context(), req)
		assert.NilError(t, err)
		_ = updated
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateTensorboardRequest{}
			req.SetTensorboard(msg)
			_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateTensorboardRequest{}
			req.SetTensorboard(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Tensorboard)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateTensorboardRequest{}
				req.SetTensorboard(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
			t.Run(".encryption_spec.kms_key_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*Tensorboard)
				container := msg.GetEncryptionSpec()
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("kms_key_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateTensorboardRequest{}
				req.SetTensorboard(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateTensorboard(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListTensorboardsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListTensorboards(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListTensorboards(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListTensorboards(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Tensorboard, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListTensorboards(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetTensorboards(),
				cmpopts.SortSlices(func(a, b *Tensorboard) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListTensorboards(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListTensorboards(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Tensorboard, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListTensorboards(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetTensorboards()))
				msgs = append(msgs, response.GetTensorboards()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Tensorboard) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteTensorboardRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteTensorboard(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListTensorboardsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListTensorboards(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetTensorboards(),
				cmpopts.SortSlices(func(a, b *Tensorboard) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Tensorboard, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Tensorboard, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardsRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListTensorboards(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetTensorboards()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Tensorboard) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteTensorboard(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteTensorboard(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteTensorboardRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteTensorboard(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteTensorboardRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteTensorboard(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-")
		_, err := fx.Service().DeleteTensorboard(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *TensorboardServiceTensorboardTestSuiteConfig) create(t *testing.T, parent string) *Tensorboard {
	t.Helper()
	t.Skip("Long running create method not supported")
	return nil
}

type TensorboardServiceTensorboardExperimentTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() TensorboardServiceServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *TensorboardExperiment
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *TensorboardExperiment
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardExperimentRequest{}
		req.SetParent("")
		req.SetTensorboardExperiment(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardExperimentRequest{}
		req.SetParent("invalid resource name")
		req.SetTensorboardExperiment(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		req := &CreateTensorboardExperimentRequest{}
		req.SetParent(parent)
		req.SetTensorboardExperiment(fx.Create(parent))
		msg, err := fx.Service().CreateTensorboardExperiment(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, msg.GetCreateTime() != nil)
		assert.Check(t, !msg.GetCreateTime().AsTime().IsZero())
		assert.Check(t, msg.GetCreateTime().AsTime().After(beforeCreate), "msg.GetCreateTime()(%v) is not after beforeCreate (%v)", msg.GetCreateTime().AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		getReq := &CreateTensorboardExperimentRequest{}
		getReq.SetParent(parent)
		getReq.SetTensorboardExperiment(fx.Create(parent))
		msg, err := fx.Service().CreateTensorboardExperiment(fx.Context(), getReq)
		assert.NilError(t, err)
		createReq := &GetTensorboardExperimentRequest{}
		createReq.SetName(msg.GetName())
		persisted, err := fx.Service().GetTensorboardExperiment(fx.Context(), createReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// Field etag should be populated when the resource is created.
	t.Run("etag populated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &CreateTensorboardExperimentRequest{}
		req.SetParent(parent)
		req.SetTensorboardExperiment(fx.Create(parent))
		created, err := fx.Service().CreateTensorboardExperiment(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, created.GetEtag() != "")
	})

}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardExperimentRequest{}
		req.SetName("")
		_, err := fx.Service().GetTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardExperimentRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardExperimentRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetTensorboardExperiment(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardExperimentRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardExperimentRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-/experiments/-")
		_, err := fx.Service().GetTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateTensorboardExperimentRequest{}
		req.SetTensorboardExperiment(msg)
		_, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateTensorboardExperimentRequest{}
		req.SetTensorboardExperiment(msg)
		_, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field update_time should be updated when the resource is updated.
	t.Run("update time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &UpdateTensorboardExperimentRequest{}
		req.SetTensorboardExperiment(created)
		updated, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetUpdateTime().AsTime().After(created.GetUpdateTime().AsTime()))
	})

	// The updated resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		updateReq := &UpdateTensorboardExperimentRequest{}
		updateReq.SetTensorboardExperiment(created)
		updated, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), updateReq)
		assert.NilError(t, err)
		getReq := &GetTensorboardExperimentRequest{}
		getReq.SetName(updated.GetName())
		persisted, err := fx.Service().GetTensorboardExperiment(fx.Context(), getReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, updated, persisted, protocmp.Transform())
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		msg.SetEtag(`"99999"`)
		req := &UpdateTensorboardExperimentRequest{}
		req.SetTensorboardExperiment(msg)
		_, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		msg.SetEtag(created.GetEtag())
		req := &UpdateTensorboardExperimentRequest{}
		req.SetTensorboardExperiment(msg)
		updated, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetEtag() != created.GetEtag())
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateTensorboardExperimentRequest{}
			req.SetTensorboardExperiment(msg)
			_, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateTensorboardExperimentRequest{}
			req.SetTensorboardExperiment(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateTensorboardExperiment(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

	}
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListTensorboardExperimentsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardExperimentsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardExperimentsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*TensorboardExperiment, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardExperimentsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetTensorboardExperiments(),
				cmpopts.SortSlices(func(a, b *TensorboardExperiment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardExperimentsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardExperimentsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*TensorboardExperiment, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardExperimentsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetTensorboardExperiments()))
				msgs = append(msgs, response.GetTensorboardExperiments()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *TensorboardExperiment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteTensorboardExperimentRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListTensorboardExperimentsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListTensorboardExperiments(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetTensorboardExperiments(),
				cmpopts.SortSlices(func(a, b *TensorboardExperiment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*TensorboardExperiment, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*TensorboardExperiment, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardExperimentsRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListTensorboardExperiments(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetTensorboardExperiments()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *TensorboardExperiment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardExperimentRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardExperimentRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardExperimentRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardExperimentRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteTensorboardExperimentRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteTensorboardExperimentRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteTensorboardExperiment(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardExperimentRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-/experiments/-")
		_, err := fx.Service().DeleteTensorboardExperiment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *TensorboardServiceTensorboardExperimentTestSuiteConfig) create(t *testing.T, parent string) *TensorboardExperiment {
	t.Helper()
	req := &CreateTensorboardExperimentRequest{}
	req.SetParent(parent)
	req.SetTensorboardExperiment(fx.Create(parent))
	created, err := fx.Service().CreateTensorboardExperiment(fx.Context(), req)
	assert.NilError(t, err)
	return created
}

type TensorboardServiceTensorboardRunTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() TensorboardServiceServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *TensorboardRun
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *TensorboardRun
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardRunRequest{}
		req.SetParent("")
		req.SetTensorboardRun(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardRunRequest{}
		req.SetParent("invalid resource name")
		req.SetTensorboardRun(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		req := &CreateTensorboardRunRequest{}
		req.SetParent(parent)
		req.SetTensorboardRun(fx.Create(parent))
		msg, err := fx.Service().CreateTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, msg.GetCreateTime() != nil)
		assert.Check(t, !msg.GetCreateTime().AsTime().IsZero())
		assert.Check(t, msg.GetCreateTime().AsTime().After(beforeCreate), "msg.GetCreateTime()(%v) is not after beforeCreate (%v)", msg.GetCreateTime().AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		getReq := &CreateTensorboardRunRequest{}
		getReq.SetParent(parent)
		getReq.SetTensorboardRun(fx.Create(parent))
		msg, err := fx.Service().CreateTensorboardRun(fx.Context(), getReq)
		assert.NilError(t, err)
		createReq := &GetTensorboardRunRequest{}
		createReq.SetName(msg.GetName())
		persisted, err := fx.Service().GetTensorboardRun(fx.Context(), createReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateTensorboardRunRequest{}
			req.SetParent(parent)
			req.SetTensorboardRun(msg)
			_, err := fx.Service().CreateTensorboardRun(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

	// Field etag should be populated when the resource is created.
	t.Run("etag populated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &CreateTensorboardRunRequest{}
		req.SetParent(parent)
		req.SetTensorboardRun(fx.Create(parent))
		created, err := fx.Service().CreateTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, created.GetEtag() != "")
	})

}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardRunRequest{}
		req.SetName("")
		_, err := fx.Service().GetTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardRunRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardRunRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardRunRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardRunRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-/experiments/-/runs/-")
		_, err := fx.Service().GetTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateTensorboardRunRequest{}
		req.SetTensorboardRun(msg)
		_, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateTensorboardRunRequest{}
		req.SetTensorboardRun(msg)
		_, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field update_time should be updated when the resource is updated.
	t.Run("update time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &UpdateTensorboardRunRequest{}
		req.SetTensorboardRun(created)
		updated, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetUpdateTime().AsTime().After(created.GetUpdateTime().AsTime()))
	})

	// The updated resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		updateReq := &UpdateTensorboardRunRequest{}
		updateReq.SetTensorboardRun(created)
		updated, err := fx.Service().UpdateTensorboardRun(fx.Context(), updateReq)
		assert.NilError(t, err)
		getReq := &GetTensorboardRunRequest{}
		getReq.SetName(updated.GetName())
		persisted, err := fx.Service().GetTensorboardRun(fx.Context(), getReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, updated, persisted, protocmp.Transform())
	})

	// The field create_time should be preserved when a '*'-update mask is used.
	t.Run("preserve create_time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		originalCreateTime := created.GetCreateTime()
		req := &UpdateTensorboardRunRequest{}
		req.SetTensorboardRun(created)
		req.SetUpdateMask(&fieldmaskpb.FieldMask{
			Paths: []string{
				"*",
			},
		})
		updated, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, originalCreateTime, updated.GetCreateTime(), protocmp.Transform())
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		msg.SetEtag(`"99999"`)
		req := &UpdateTensorboardRunRequest{}
		req.SetTensorboardRun(msg)
		_, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		msg.SetEtag(created.GetEtag())
		req := &UpdateTensorboardRunRequest{}
		req.SetTensorboardRun(msg)
		updated, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetEtag() != created.GetEtag())
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateTensorboardRunRequest{}
			req.SetTensorboardRun(msg)
			_, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateTensorboardRunRequest{}
			req.SetTensorboardRun(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*TensorboardRun)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateTensorboardRunRequest{}
				req.SetTensorboardRun(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateTensorboardRun(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListTensorboardRunsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardRunsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardRunsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*TensorboardRun, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardRunsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetTensorboardRuns(),
				cmpopts.SortSlices(func(a, b *TensorboardRun) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardRunsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardRunsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*TensorboardRun, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardRunsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetTensorboardRuns()))
				msgs = append(msgs, response.GetTensorboardRuns()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *TensorboardRun) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteTensorboardRunRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteTensorboardRun(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListTensorboardRunsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListTensorboardRuns(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetTensorboardRuns(),
				cmpopts.SortSlices(func(a, b *TensorboardRun) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*TensorboardRun, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*TensorboardRun, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardRunsRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListTensorboardRuns(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetTensorboardRuns()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *TensorboardRun) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardRunRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardRunRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardRunRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteTensorboardRun(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardRunRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteTensorboardRunRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteTensorboardRun(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteTensorboardRunRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteTensorboardRun(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardRunRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-/experiments/-/runs/-")
		_, err := fx.Service().DeleteTensorboardRun(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *TensorboardServiceTensorboardRunTestSuiteConfig) create(t *testing.T, parent string) *TensorboardRun {
	t.Helper()
	req := &CreateTensorboardRunRequest{}
	req.SetParent(parent)
	req.SetTensorboardRun(fx.Create(parent))
	created, err := fx.Service().CreateTensorboardRun(fx.Context(), req)
	assert.NilError(t, err)
	return created
}

type TensorboardServiceTensorboardTimeSeriesTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() TensorboardServiceServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *TensorboardTimeSeries
	// Update should return a resource which is valid to update, i.e.
	// all required fields set.
	Update func(parent string) *TensorboardTimeSeries
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("Update", fx.testUpdate)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardTimeSeriesRequest{}
		req.SetParent("")
		req.SetTensorboardTimeSeries(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateTensorboardTimeSeriesRequest{}
		req.SetParent("invalid resource name")
		req.SetTensorboardTimeSeries(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field create_time should be populated when the resource is created.
	t.Run("create time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		beforeCreate := time.Now()
		req := &CreateTensorboardTimeSeriesRequest{}
		req.SetParent(parent)
		req.SetTensorboardTimeSeries(fx.Create(parent))
		msg, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, msg.GetCreateTime() != nil)
		assert.Check(t, !msg.GetCreateTime().AsTime().IsZero())
		assert.Check(t, msg.GetCreateTime().AsTime().After(beforeCreate), "msg.GetCreateTime()(%v) is not after beforeCreate (%v)", msg.GetCreateTime().AsTime(), beforeCreate)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		getReq := &CreateTensorboardTimeSeriesRequest{}
		getReq.SetParent(parent)
		getReq.SetTensorboardTimeSeries(fx.Create(parent))
		msg, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), getReq)
		assert.NilError(t, err)
		createReq := &GetTensorboardTimeSeriesRequest{}
		createReq.SetName(msg.GetName())
		persisted, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), createReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".display_name", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
			container.ProtoReflect().Clear(fd)
			req := &CreateTensorboardTimeSeriesRequest{}
			req.SetParent(parent)
			req.SetTensorboardTimeSeries(msg)
			_, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".value_type", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("value_type")
			container.ProtoReflect().Clear(fd)
			req := &CreateTensorboardTimeSeriesRequest{}
			req.SetParent(parent)
			req.SetTensorboardTimeSeries(msg)
			_, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

	// Field etag should be populated when the resource is created.
	t.Run("etag populated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &CreateTensorboardTimeSeriesRequest{}
		req.SetParent(parent)
		req.SetTensorboardTimeSeries(fx.Create(parent))
		created, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, created.GetEtag() != "")
	})

}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardTimeSeriesRequest{}
		req.SetName("")
		_, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardTimeSeriesRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardTimeSeriesRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetTensorboardTimeSeriesRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetTensorboardTimeSeriesRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-/experiments/-/runs/-/timeSeries/-")
		_, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) testUpdate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("")
		req := &UpdateTensorboardTimeSeriesRequest{}
		req.SetTensorboardTimeSeries(msg)
		_, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		msg := fx.Update(parent)
		msg.SetName("invalid resource name")
		req := &UpdateTensorboardTimeSeriesRequest{}
		req.SetTensorboardTimeSeries(msg)
		_, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Field update_time should be updated when the resource is updated.
	t.Run("update time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &UpdateTensorboardTimeSeriesRequest{}
		req.SetTensorboardTimeSeries(created)
		updated, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetUpdateTime().AsTime().After(created.GetUpdateTime().AsTime()))
	})

	// The updated resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		updateReq := &UpdateTensorboardTimeSeriesRequest{}
		updateReq.SetTensorboardTimeSeries(created)
		updated, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), updateReq)
		assert.NilError(t, err)
		getReq := &GetTensorboardTimeSeriesRequest{}
		getReq.SetName(updated.GetName())
		persisted, err := fx.Service().GetTensorboardTimeSeries(fx.Context(), getReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, updated, persisted, protocmp.Transform())
	})

	// The field create_time should be preserved when a '*'-update mask is used.
	t.Run("preserve create_time", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		originalCreateTime := created.GetCreateTime()
		req := &UpdateTensorboardTimeSeriesRequest{}
		req.SetTensorboardTimeSeries(created)
		req.SetUpdateMask(&fieldmaskpb.FieldMask{
			Paths: []string{
				"*",
			},
		})
		updated, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, originalCreateTime, updated.GetCreateTime(), protocmp.Transform())
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		msg.SetEtag(`"99999"`)
		req := &UpdateTensorboardTimeSeriesRequest{}
		req.SetTensorboardTimeSeries(msg)
		_, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

	// Field etag should have a new value when the resource is successfully updated.
	t.Run("etag updated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		msg := fx.Update(parent)
		msg.SetName(created.GetName())
		msg.SetEtag(created.GetEtag())
		req := &UpdateTensorboardTimeSeriesRequest{}
		req.SetTensorboardTimeSeries(msg)
		updated, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, updated.GetEtag() != created.GetEtag())
	})

	{
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		// Method should fail with NotFound if the resource does not exist.
		t.Run("not found", func(t *testing.T) {
			fx.maybeSkip(t)
			msg := fx.Update(parent)
			msg.SetName(created.GetName() + "notfound")
			req := &UpdateTensorboardTimeSeriesRequest{}
			req.SetTensorboardTimeSeries(msg)
			_, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
			assert.Equal(t, codes.NotFound, status.Code(err), err)
		})

		// The method should fail with InvalidArgument if the update_mask is invalid.
		t.Run("invalid update mask", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &UpdateTensorboardTimeSeriesRequest{}
			req.SetTensorboardTimeSeries(created)
			req.SetUpdateMask(&fieldmaskpb.FieldMask{
				Paths: []string{
					"invalid_field_xyz",
				},
			})
			_, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})

		// Method should fail with InvalidArgument if any required field is missing
		// when called with '*' update_mask.
		t.Run("required fields", func(t *testing.T) {
			fx.maybeSkip(t)
			t.Run(".display_name", func(t *testing.T) {
				fx.maybeSkip(t)
				msg := proto.Clone(created).(*TensorboardTimeSeries)
				container := msg
				if container == nil {
					t.Skip("not reachable")
				}
				fd := container.ProtoReflect().Descriptor().Fields().ByName("display_name")
				container.ProtoReflect().Clear(fd)
				req := &UpdateTensorboardTimeSeriesRequest{}
				req.SetTensorboardTimeSeries(msg)
				req.SetUpdateMask(&fieldmaskpb.FieldMask{
					Paths: []string{
						"*",
					},
				})
				_, err := fx.Service().UpdateTensorboardTimeSeries(fx.Context(), req)
				assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
			})
		})

	}
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListTensorboardTimeSeriesRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardTimeSeriesRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListTensorboardTimeSeriesRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*TensorboardTimeSeries, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardTimeSeriesRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetTensorboardTimeSeries(),
				cmpopts.SortSlices(func(a, b *TensorboardTimeSeries) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardTimeSeriesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListTensorboardTimeSeriesRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*TensorboardTimeSeries, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardTimeSeriesRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetTensorboardTimeSeries()))
				msgs = append(msgs, response.GetTensorboardTimeSeries()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *TensorboardTimeSeries) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteTensorboardTimeSeriesRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListTensorboardTimeSeriesRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetTensorboardTimeSeries(),
				cmpopts.SortSlices(func(a, b *TensorboardTimeSeries) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*TensorboardTimeSeries, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*TensorboardTimeSeries, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListTensorboardTimeSeriesRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListTensorboardTimeSeries(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetTensorboardTimeSeries()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *TensorboardTimeSeries) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardTimeSeriesRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardTimeSeriesRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardTimeSeriesRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteTensorboardTimeSeriesRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteTensorboardTimeSeriesRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteTensorboardTimeSeriesRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteTensorboardTimeSeriesRequest{}
		req.SetName("projects/-/locations/-/tensorboards/-/experiments/-/runs/-/timeSeries/-")
		_, err := fx.Service().DeleteTensorboardTimeSeries(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *TensorboardServiceTensorboardTimeSeriesTestSuiteConfig) create(t *testing.T, parent string) *TensorboardTimeSeries {
	t.Helper()
	req := &CreateTensorboardTimeSeriesRequest{}
	req.SetParent(parent)
	req.SetTensorboardTimeSeries(fx.Create(parent))
	created, err := fx.Service().CreateTensorboardTimeSeries(fx.Context(), req)
	assert.NilError(t, err)
	return created
}
