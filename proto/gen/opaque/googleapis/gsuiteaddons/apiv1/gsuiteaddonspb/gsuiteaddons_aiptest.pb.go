// Code generated by protoc-gen-go-aip-test. DO NOT EDIT.

package gsuiteaddonspb

import (
	context "context"
	cmpopts "github.com/google/go-cmp/cmp/cmpopts"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	protocmp "google.golang.org/protobuf/testing/protocmp"
	assert "gotest.tools/v3/assert"
	strings "strings"
	testing "testing"
)

// GSuiteAddOnsTestSuiteConfigProvider is the interface to implement to decide which resources
// that should be tested and how it's configured.
type GSuiteAddOnsTestSuiteConfigProvider interface {
	// GSuiteAddOnsAuthorization should return a config, or nil, which means that the tests will be skipped.
	GSuiteAddOnsAuthorization(t *testing.T) *GSuiteAddOnsAuthorizationTestSuiteConfig
	// GSuiteAddOnsDeployment should return a config, or nil, which means that the tests will be skipped.
	GSuiteAddOnsDeployment(t *testing.T) *GSuiteAddOnsDeploymentTestSuiteConfig
	// GSuiteAddOnsInstallStatus should return a config, or nil, which means that the tests will be skipped.
	GSuiteAddOnsInstallStatus(t *testing.T) *GSuiteAddOnsInstallStatusTestSuiteConfig
}

// testGSuiteAddOns is the main entrypoint for starting the AIP tests.
func testGSuiteAddOns(t *testing.T, s GSuiteAddOnsTestSuiteConfigProvider) {
	testGSuiteAddOnsAuthorization(t, s)
	testGSuiteAddOnsDeployment(t, s)
	testGSuiteAddOnsInstallStatus(t, s)
}

func testGSuiteAddOnsAuthorization(t *testing.T, s GSuiteAddOnsTestSuiteConfigProvider) {
	t.Run("Authorization", func(t *testing.T) {
		config := s.GSuiteAddOnsAuthorization(t)
		if config == nil {
			t.Skip("Method GSuiteAddOnsAuthorization not implemented")
		}
		if config.Service == nil {
			t.Skip("Method GSuiteAddOnsAuthorization.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testGSuiteAddOnsDeployment(t *testing.T, s GSuiteAddOnsTestSuiteConfigProvider) {
	t.Run("Deployment", func(t *testing.T) {
		config := s.GSuiteAddOnsDeployment(t)
		if config == nil {
			t.Skip("Method GSuiteAddOnsDeployment not implemented")
		}
		if config.Service == nil {
			t.Skip("Method GSuiteAddOnsDeployment.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

func testGSuiteAddOnsInstallStatus(t *testing.T, s GSuiteAddOnsTestSuiteConfigProvider) {
	t.Run("InstallStatus", func(t *testing.T) {
		config := s.GSuiteAddOnsInstallStatus(t)
		if config == nil {
			t.Skip("Method GSuiteAddOnsInstallStatus not implemented")
		}
		if config.Service == nil {
			t.Skip("Method GSuiteAddOnsInstallStatus.Service() not implemented")
		}
		if config.Context == nil {
			config.Context = func() context.Context { return context.Background() }
		}
		config.test(t)
	})
}

type GSuiteAddOnsTestSuite struct {
	T *testing.T
	// Server to test.
	Server GSuiteAddOnsServer
}

func (fx GSuiteAddOnsTestSuite) TestAuthorization(ctx context.Context, options GSuiteAddOnsAuthorizationTestSuiteConfig) {
	fx.T.Run("Authorization", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() GSuiteAddOnsServer { return fx.Server }
		options.test(t)
	})
}

func (fx GSuiteAddOnsTestSuite) TestDeployment(ctx context.Context, options GSuiteAddOnsDeploymentTestSuiteConfig) {
	fx.T.Run("Deployment", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() GSuiteAddOnsServer { return fx.Server }
		options.test(t)
	})
}

func (fx GSuiteAddOnsTestSuite) TestInstallStatus(ctx context.Context, options GSuiteAddOnsInstallStatusTestSuiteConfig) {
	fx.T.Run("InstallStatus", func(t *testing.T) {
		options.Context = func() context.Context { return ctx }
		options.Service = func() GSuiteAddOnsServer { return fx.Server }
		options.test(t)
	})
}

type GSuiteAddOnsAuthorizationTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() GSuiteAddOnsServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// CreateResource should create a Authorization and return it.
	// If the field is not set, some tests will be skipped.
	//
	// This method is generated because service does not expose a Create
	// method (or it does not comply with AIP).
	CreateResource func(ctx context.Context) (*Authorization, error)
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *GSuiteAddOnsAuthorizationTestSuiteConfig) test(t *testing.T) {
	t.Run("Get", fx.testGet)
}

func (fx *GSuiteAddOnsAuthorizationTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetAuthorizationRequest{}
		req.SetName("")
		_, err := fx.Service().GetAuthorization(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetAuthorizationRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetAuthorization(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		created := fx.create(t)
		req := &GetAuthorizationRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetAuthorization(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetAuthorizationRequest{}
		req.SetName("projects/-/authorization")
		_, err := fx.Service().GetAuthorization(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *GSuiteAddOnsAuthorizationTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *GSuiteAddOnsAuthorizationTestSuiteConfig) create(t *testing.T) *Authorization {
	t.Helper()
	if fx.CreateResource == nil {
		t.Skip("Test skipped because CreateResource not specified on GSuiteAddOnsAuthorizationTestSuiteConfig")
	}
	created, err := fx.CreateResource(fx.Context())
	assert.NilError(t, err)
	return created
}

type GSuiteAddOnsDeploymentTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() GSuiteAddOnsServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// Create should return a resource which is valid to create, i.e.
	// all required fields set.
	Create func(parent string) *Deployment
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) test(t *testing.T) {
	t.Run("Create", fx.testCreate)
	t.Run("Get", fx.testGet)
	t.Run("List", fx.testList)
	t.Run("Delete", fx.testDelete)
}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) testCreate(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no parent is provided.
	t.Run("missing parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateDeploymentRequest{}
		req.SetParent("")
		req.SetDeployment(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &CreateDeploymentRequest{}
		req.SetParent("invalid resource name")
		req.SetDeployment(fx.Create(fx.nextParent(t, false)))
		_, err := fx.Service().CreateDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// The created resource should be persisted and reachable with Get.
	t.Run("persisted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		getReq := &CreateDeploymentRequest{}
		getReq.SetParent(parent)
		getReq.SetDeployment(fx.Create(parent))
		msg, err := fx.Service().CreateDeployment(fx.Context(), getReq)
		assert.NilError(t, err)
		createReq := &GetDeploymentRequest{}
		createReq.SetName(msg.GetName())
		persisted, err := fx.Service().GetDeployment(fx.Context(), createReq)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, persisted, protocmp.Transform())
	})

	// The method should fail with InvalidArgument if the resource has any
	// required fields and they are not provided.
	t.Run("required fields", func(t *testing.T) {
		fx.maybeSkip(t)
		t.Run(".add_ons.calendar.event_open_trigger.run_function", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAddOns().GetCalendar().GetEventOpenTrigger()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("run_function")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentRequest{}
			req.SetParent(parent)
			req.SetDeployment(msg)
			_, err := fx.Service().CreateDeployment(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".add_ons.docs.on_file_scope_granted_trigger.run_function", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAddOns().GetDocs().GetOnFileScopeGrantedTrigger()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("run_function")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentRequest{}
			req.SetParent(parent)
			req.SetDeployment(msg)
			_, err := fx.Service().CreateDeployment(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".add_ons.sheets.on_file_scope_granted_trigger.run_function", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAddOns().GetSheets().GetOnFileScopeGrantedTrigger()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("run_function")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentRequest{}
			req.SetParent(parent)
			req.SetDeployment(msg)
			_, err := fx.Service().CreateDeployment(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
		t.Run(".add_ons.slides.on_file_scope_granted_trigger.run_function", func(t *testing.T) {
			fx.maybeSkip(t)
			parent := fx.nextParent(t, false)
			msg := fx.Create(parent)
			container := msg.GetAddOns().GetSlides().GetOnFileScopeGrantedTrigger()
			if container == nil {
				t.Skip("not reachable")
			}
			fd := container.ProtoReflect().Descriptor().Fields().ByName("run_function")
			container.ProtoReflect().Clear(fd)
			req := &CreateDeploymentRequest{}
			req.SetParent(parent)
			req.SetDeployment(msg)
			_, err := fx.Service().CreateDeployment(fx.Context(), req)
			assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
		})
	})

	// Field etag should be populated when the resource is created.
	t.Run("etag populated", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &CreateDeploymentRequest{}
		req.SetParent(parent)
		req.SetDeployment(fx.Create(parent))
		created, err := fx.Service().CreateDeployment(fx.Context(), req)
		assert.NilError(t, err)
		assert.Check(t, created.GetEtag() != "")
	})

}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDeploymentRequest{}
		req.SetName("")
		_, err := fx.Service().GetDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDeploymentRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetDeploymentRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetDeployment(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetDeploymentRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().GetDeployment(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetDeploymentRequest{}
		req.SetName("projects/-/deployments/-")
		_, err := fx.Service().GetDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) testList(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if provided parent is invalid.
	t.Run("invalid parent", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &ListDeploymentsRequest{}
		req.SetParent("invalid resource name")
		_, err := fx.Service().ListDeployments(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page token is not valid.
	t.Run("invalid page token", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDeploymentsRequest{}
		req.SetParent(parent)
		req.SetPageToken("invalid page token")
		_, err := fx.Service().ListDeployments(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument is provided page size is negative.
	t.Run("negative page size", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		req := &ListDeploymentsRequest{}
		req.SetParent(parent)
		req.SetPageSize(-10)
		_, err := fx.Service().ListDeployments(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	{
		const resourcesCount = 15
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Deployment, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// If parent is provided the method must only return resources
		// under that parent.
		t.Run("isolation", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentsRequest{}
			req.SetParent(parent)
			req.SetPageSize(999)
			response, err := fx.Service().ListDeployments(fx.Context(), req)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs,
				response.GetDeployments(),
				cmpopts.SortSlices(func(a, b *Deployment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// If there are no more resources, next_page_token should not be set.
		t.Run("last page", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount)
			response, err := fx.Service().ListDeployments(fx.Context(), req)
			assert.NilError(t, err)
			assert.Equal(t, "", response.GetNextPageToken())
		})

		// If there are more resources, next_page_token should be set.
		t.Run("more pages", func(t *testing.T) {
			fx.maybeSkip(t)
			req := &ListDeploymentsRequest{}
			req.SetParent(parent)
			req.SetPageSize(resourcesCount - 1)
			response, err := fx.Service().ListDeployments(fx.Context(), req)
			assert.NilError(t, err)
			assert.Check(t, response.GetNextPageToken() != "")
		})

		// Listing resource one by one should eventually return all resources.
		t.Run("one by one", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Deployment, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDeploymentsRequest{}
				req.SetParent(parent)
				req.SetPageSize(1)
				req.SetPageToken(nextPageToken)
				response, err := fx.Service().ListDeployments(fx.Context(), req)
				assert.NilError(t, err)
				assert.Equal(t, 1, len(response.GetDeployments()))
				msgs = append(msgs, response.GetDeployments()...)
				nextPageToken = response.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Deployment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

		// Method should not return deleted resources.
		t.Run("deleted", func(t *testing.T) {
			fx.maybeSkip(t)
			const deleteCount = 5
			for i := 0; i < deleteCount; i++ {
				delReq := &DeleteDeploymentRequest{}
				delReq.SetName(parentMsgs[i].GetName())
				_, err := fx.Service().DeleteDeployment(fx.Context(), delReq)
				assert.NilError(t, err)
			}
			listReq := &ListDeploymentsRequest{}
			listReq.SetParent(parent)
			listReq.SetPageSize(9999)
			response, err := fx.Service().ListDeployments(fx.Context(), listReq)
			assert.NilError(t, err)
			assert.DeepEqual(
				t,
				parentMsgs[deleteCount:],
				response.GetDeployments(),
				cmpopts.SortSlices(func(a, b *Deployment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
	{
		const resourcesCount = 101
		parent := fx.nextParent(t, true)
		parentMsgs := make([]*Deployment, resourcesCount)
		for i := 0; i < resourcesCount; i++ {
			parentMsgs[i] = fx.create(t, parent)
		}

		// Listing resource with page size zero should eventually return all resources.
		t.Run("page size zero", func(t *testing.T) {
			fx.maybeSkip(t)
			msgs := make([]*Deployment, 0, resourcesCount)
			var nextPageToken string
			for {
				req := &ListDeploymentsRequest{}
				req.SetParent(parent)
				req.SetPageSize(0)
				req.SetPageToken(nextPageToken)
				page, err := fx.Service().ListDeployments(fx.Context(), req)
				assert.NilError(t, err)
				msgs = append(msgs, page.GetDeployments()...)
				nextPageToken = page.GetNextPageToken()
				if nextPageToken == "" {
					break
				}
			}
			assert.DeepEqual(
				t,
				parentMsgs,
				msgs,
				cmpopts.SortSlices(func(a, b *Deployment) bool {
					return a.GetName() < b.GetName()
				}),
				protocmp.Transform(),
			)
		})

	}
}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) testDelete(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteDeploymentRequest{}
		req.SetName("")
		_, err := fx.Service().DeleteDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteDeploymentRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().DeleteDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be deleted without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteDeploymentRequest{}
		req.SetName(created.GetName())
		_, err := fx.Service().DeleteDeployment(fx.Context(), req)
		assert.NilError(t, err)
	})

	// Method should fail with NotFound if the resource does not exist.
	t.Run("not found", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteDeploymentRequest{}
		req.SetName(created.GetName() + "notfound")
		_, err := fx.Service().DeleteDeployment(fx.Context(), req)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with NotFound if the resource was already deleted. This also applies to soft-deletion.
	t.Run("already deleted", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req1 := &DeleteDeploymentRequest{}
		req1.SetName(created.GetName())
		deleted, err := fx.Service().DeleteDeployment(fx.Context(), req1)
		assert.NilError(t, err)
		_ = deleted
		req2 := &DeleteDeploymentRequest{}
		req2.SetName(created.GetName())
		_, err = fx.Service().DeleteDeployment(fx.Context(), req2)
		assert.Equal(t, codes.NotFound, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &DeleteDeploymentRequest{}
		req.SetName("projects/-/deployments/-")
		_, err := fx.Service().DeleteDeployment(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with Aborted if the supplied etag doesnt match the current etag value.
	t.Run("etag mismatch", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &DeleteDeploymentRequest{}
		req.SetName(created.GetName())
		req.SetEtag(`"99999"`)
		_, err := fx.Service().DeleteDeployment(fx.Context(), req)
		assert.Equal(t, codes.Aborted, status.Code(err), err)
	})

}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *GSuiteAddOnsDeploymentTestSuiteConfig) create(t *testing.T, parent string) *Deployment {
	t.Helper()
	req := &CreateDeploymentRequest{}
	req.SetParent(parent)
	req.SetDeployment(fx.Create(parent))
	created, err := fx.Service().CreateDeployment(fx.Context(), req)
	assert.NilError(t, err)
	return created
}

type GSuiteAddOnsInstallStatusTestSuiteConfig struct {
	currParent int

	// Service should return the service that should be tested.
	// The service will be used for several tests.
	Service func() GSuiteAddOnsServer
	// Context should return a new context.
	// The context will be used for several tests.
	Context func() context.Context
	// The parents to use when creating resources.
	// At least one parent needs to be set. Depending on methods available on the resource,
	// more may be required. If insufficient number of parents are
	// provided the test will fail.
	Parents []string
	// CreateResource should create a InstallStatus and return it.
	// If the field is not set, some tests will be skipped.
	//
	// This method is generated because service does not expose a Create
	// method (or it does not comply with AIP).
	CreateResource func(ctx context.Context, parent string) (*InstallStatus, error)
	// Patterns of tests to skip.
	// For example if a service has a Get method:
	// Skip: ["Get"] will skip all tests for Get.
	// Skip: ["Get/persisted"] will only skip the subtest called "persisted" of Get.
	Skip []string
}

func (fx *GSuiteAddOnsInstallStatusTestSuiteConfig) test(t *testing.T) {
	t.Run("Get", fx.testGet)
}

func (fx *GSuiteAddOnsInstallStatusTestSuiteConfig) testGet(t *testing.T) {
	fx.maybeSkip(t)
	// Method should fail with InvalidArgument if no name is provided.
	t.Run("missing name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstallStatusRequest{}
		req.SetName("")
		_, err := fx.Service().GetInstallStatus(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Method should fail with InvalidArgument if the provided name is not valid.
	t.Run("invalid name", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstallStatusRequest{}
		req.SetName("invalid resource name")
		_, err := fx.Service().GetInstallStatus(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

	// Resource should be returned without errors if it exists.
	t.Run("exists", func(t *testing.T) {
		fx.maybeSkip(t)
		parent := fx.nextParent(t, false)
		created := fx.create(t, parent)
		req := &GetInstallStatusRequest{}
		req.SetName(created.GetName())
		msg, err := fx.Service().GetInstallStatus(fx.Context(), req)
		assert.NilError(t, err)
		assert.DeepEqual(t, msg, created, protocmp.Transform())
	})

	// Method should fail with InvalidArgument if the provided name only contains wildcards ('-')
	t.Run("only wildcards", func(t *testing.T) {
		fx.maybeSkip(t)
		req := &GetInstallStatusRequest{}
		req.SetName("projects/-/deployments/-/installStatus")
		_, err := fx.Service().GetInstallStatus(fx.Context(), req)
		assert.Equal(t, codes.InvalidArgument, status.Code(err), err)
	})

}

func (fx *GSuiteAddOnsInstallStatusTestSuiteConfig) nextParent(t *testing.T, pristine bool) string {
	if pristine {
		fx.currParent++
	}
	if fx.currParent >= len(fx.Parents) {
		t.Fatal("need at least", fx.currParent+1, "parents")
	}
	return fx.Parents[fx.currParent]
}

func (fx *GSuiteAddOnsInstallStatusTestSuiteConfig) peekNextParent(t *testing.T) string {
	next := fx.currParent + 1
	if next >= len(fx.Parents) {
		t.Fatal("need at least", next+1, "parents")
	}
	return fx.Parents[next]
}

func (fx *GSuiteAddOnsInstallStatusTestSuiteConfig) maybeSkip(t *testing.T) {
	for _, skip := range fx.Skip {
		if strings.Contains(t.Name(), skip) {
			t.Skip("skipped because of .Skip")
		}
	}
}

func (fx *GSuiteAddOnsInstallStatusTestSuiteConfig) create(t *testing.T, parent string) *InstallStatus {
	t.Helper()
	if fx.CreateResource == nil {
		t.Skip("Test skipped because CreateResource not specified on GSuiteAddOnsInstallStatusTestSuiteConfig")
	}
	created, err := fx.CreateResource(fx.Context(), parent)
	assert.NilError(t, err)
	return created
}
